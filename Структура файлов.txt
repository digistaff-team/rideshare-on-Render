project_root/
├── .env                      # уже есть
├── main.py                   # уже есть
├── requirements.txt          # ОБЯЗАТЕЛЬНО НУЖЕН
├── Dockerfile                # рекомендуется для деплоя
├── docker-compose.yml        # рекомендуется, если используете PostgreSQL локально
└── src/
    ├── __init__.py           # пустой файл, чтобы Python видел src как пакет
    ├── bot/
    │   ├── __init__.py       # пустой
    │   └── handlers.py       # уже есть
    ├── database/
    │   ├── __init__.py       # пустой
    │   ├── session.py        # уже есть
    │   └── models.py         # уже есть
    └── services/
        ├── __init__.py       # пустой
        └── nlu.py            # уже есть